/** App config — ubah di sini untuk environment */
export const Config = {
  /** Base path API (lokal & Vercel sama) */
  apiBase: "/api",
  /** Proxy gambar */
  imgProxy: "/img",
  /** Item per halaman list */
  pageSize: 20,
  /** Chapter preview di kartu */
  previewChapters: 3,
  /** Cache-busting (opsional) */
  version: "1.0.0",
};
